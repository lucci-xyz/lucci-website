/**
 * Navigation configuration
 * Centralized navigation links and menu structure
 */

import type { NavLink } from '@/lib/types'

// Main navigation links
export const MAIN_NAV_LINKS: NavLink[] = [
  { href: '/Work', label: 'Work' }
]
